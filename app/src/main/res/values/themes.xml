<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.FoldPaper" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Transparent system bars for Edge-to-Edge -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:enforceNavigationBarContrast" tools:targetApi="q">false</item>
        <item name="android:enforceStatusBarContrast" tools:targetApi="q">false</item>
        <item name="bottomSheetDialogTheme">@style/Theme.FoldPaper.BottomSheetDialog</item>
    </style>

    <style name="Theme.FoldPaper.BottomSheetDialog" parent="ThemeOverlay.Material3.BottomSheetDialog">
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:enforceNavigationBarContrast" tools:targetApi="q">false</item>
        <item name="enableEdgeToEdge">true</item>
    </style>

    <style name="Theme.FoldPaper.Amoled" parent="Theme.FoldPaper">
        <item name="android:colorBackground">#000000</item>
        <item name="colorSurface">#000000</item>
    </style>

</resources>
